import { IconButton } from 'react-native-paper';

import { palette } from '@Themes/index';

export const RenderScrollToBottom = () => {
  return <IconButton icon="arrow-down-bold-outline" color={palette.blue} />;
};
