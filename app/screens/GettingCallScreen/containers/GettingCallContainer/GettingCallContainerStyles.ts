import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    top: 20,
    right: 10,
    left: 10,
  },

  containerWhenExpanding: {
    position: 'relative',
    top: 0,
    right: 0,
    left: 0,
  },
});
